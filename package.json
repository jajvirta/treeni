{
  "name": "treeni",
  "version": "0.1.0",
  "description": "Motivation-first gym training log — static site (S3 + CloudFront) with a DynamoDB backend",
  "private": true,
  "scripts": {
    "dev": "npx --yes serve public",
    "deploy": "./deploy.sh"
  }
}
